apiVersion: v1
kind: Service
metadata:
  name: user-portal-service
  labels:
    app: user-portal
    component: frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: user-portal