apiVersion: v1
kind: Service
metadata:
  name: test-service
  namespace: my-namespace
spec:
  selector:
    app: test-service
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  type: ClusterIP